<table class="table table-bordered">
  <colgroup>
    <col class="span2">
    <col class="span3">
    <col class="span3">
    <col class="span3">
  </colgroup>
  <% if feedbacks.length == 0 %>
      <caption>
        <h2> Not given any feedback this week(<%=user.name %>)</h2><%= begin_date.nil? ? "" : "( #{begin_date} to #{end_date})" %>
      </caption>
  <% else %>
      <caption>
        <h2>Given Feedback List <%= user.name %> </h2><%= begin_date.nil? ? "" : "( #{begin_date} to #{end_date})" %>
      </caption>
      <thead>
      <tr style = "background-color: #A2AFCC;">
        <th>Name</th>
        <th>Assigned Date</th>
        <th>Given Date</th>
        <th>Feedback</th>
      </tr>
      </thead>
      <% feedbacks.each do |feedback| %>
          <tr>
            <td><%= user_name(feedback.to_id) %></td>
            <td><%= feedback.created_at.strftime("%a, %d-%b-%Y, %I:%M:%p") %></td>
            <td><%= date(feedback.updated_at) if !feedback.feedback.to_s.empty? %></td>
            <td><%= feedback.feedback if !feedback.feedback.to_s.empty? %></td>
          </tr>
      <% end %>
  <% end %>
</table>
<%= link_to "Back", admin_users_path, :class => "btn" %>
<%= page_navigation_links feedbacks %>